---
title: '相片處理時奇怪的方向轉置問題'
date: '2026-08-26T17:31:17+08:00'
draft: false
tags: [ "Python", "Exif", "pillow" ]
categories: [ "Package", "影像處理", "踩地雷" ]
slug: '/python-pillow-exif/'
---

## 說明

- 透過程式處理相片檔案時，寫了個透過等比例壓縮相片的功能，想要來壓縮相片，結果相片出現奇怪的方向轉置。

```shell
from PIL import Image
image_path = <你的相片檔案路徑>
img = Image.open(image_path)
w, h = img.size
compress_rate = 0.5
img_resize = img.resize((int(w * compress_rate), int(h * compress_rate)))
img_resize.save('img_resize.jpg')
```

- ![壓縮前後相片, 本研究整理](https://i.imgur.com/HzYlqv2.png)

----

## 範例

- 根據Exif 2.3官方標準文件[^Exif 2.3官方標準文件]中 (p35頁)有定義出，紀錄相片方向 (orientation)的資料欄位，其值為1~
  8，其值代表的意義節錄列點如下：
    - `1`: The 0th row is at the visual top of the image, and the 0th column is the visual left-hand side.
    - `2`: The 0th row is at the visual top of the image, and the 0th column is the visual right-hand side.
    - `3`: The 0th row is at the visual bottom of the image, and the 0th column is the visual right-hand side.
    - `4`: The 0th row is at the visual bottom of the image, and the 0th column is the visual left-hand side.
    - `5`: The 0th row is the visual left-hand side of the image, and the 0th column is the visual top.
    - `6`: The 0th row is the visual right-hand side of the image, and the 0th column is the visual top.
    - `7`: The 0th row is the visual right-hand side of the image, and The 0th column is thevisual bottom.
    - `8`: The 0th row is the visual left-hand side of the image, and the 0th column is the visual bottom.
    - `Other`: reserved

----

- 造成上述狀況的主因是相片Orientation 值為 `3` 或 `6` 或 `8`，導致系統中會"貼心的"自動校正觀看方向。
- 請參考下列圖例[^imgix URL API Documentation - Orientation]：
    - ![Orientation](https://assets.imgix.net/docs/exif_sample.png)

----

- 提供一些解決方案
    1. 調整相片方向方法程式
  ```python
  from PIL import Image
  
  
  def reorient_image(im):
      try:
          image_exif = im.getexif()
          # 注意 PIL 版本 or
          # image_exif = im._getexif()
          image_orientation = image_exif[274]
          if image_orientation in (2, '2'):
              return im.transpose(Image.FLIP_LEFT_RIGHT)
          elif image_orientation in (3, '3'):
              return im.transpose(Image.ROTATE_180)
          elif image_orientation in (4, '4'):
              return im.transpose(Image.FLIP_TOP_BOTTOM)
          elif image_orientation in (5, '5'):
              return im.transpose(Image.ROTATE_90).transpose(Image.FLIP_TOP_BOTTOM)
          elif image_orientation in (6, '6'):
              return im.transpose(Image.ROTATE_270)
          elif image_orientation in (7, '7'):
              return im.transpose(Image.ROTATE_270).transpose(Image.FLIP_TOP_BOTTOM)
          elif image_orientation in (8, '8'):
              return im.transpose(Image.ROTATE_90)
          else:
              return im
      except (KeyError, AttributeError, TypeError, IndexError):
          return im
  ```
    2. ~~握好你的拍攝裝置，確認好你要的拍攝方向、角度，頭不要歪。~~

[^Exif 2.3官方標準文件]: https://www.cipa.jp/std/documents/e/DC-008-2012_E.pdf
[^imgix URL API Documentation - Orientation]: https://assets.imgix.net/docs/exif_sample.png
